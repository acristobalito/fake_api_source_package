/// Model used to store the login params to consume the service
class LoginParamsModel {
  final String username;
  final String password;

  LoginParamsModel({required this.username, required this.password});
}
